modelVersion: "1.0"

name:
  ru: Патч
  en: Patch

sourceId: "patch"

controller:
  type: json
  config:
    idTemplate: ${targetApp!$appName|presuf('','$')}${id}
    attsValues:
      targetApp: "${targetApp!$appName}"
